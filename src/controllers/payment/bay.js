const bay = async(req, res) => {
      try {
            let {product} = req.body
            let newTrans 
      } catch (error) {
            res.status(501).json({message: "PLease try again!"})
      }
} 

module.exports = bay 